# Balance Bot environment configuration
# Copy this file to `.env` and adjust the values for your deployment.

# Local timezone (used in the Docker compose example below).
TZ=America/New_York

# Directory where Balance Bot stores persistent JSON files.
BALANCE_BOT_DATA_DIR=./data

# Optional: override the path to the persisted state file.
# Relative paths resolve from BALANCE_BOT_DATA_DIR.
# BALANCE_BOT_STATE_FILE=state.json

# Apprise endpoint used to deliver notifications.
APPRISE_API_URL=http://apprise:8000/notify

# Cron expression controlling how often balances are checked.
BALANCE_BOT_CRON=0 * * * *

# Optional: healthchecks.io ping URL for start/success/failure pings.
# HEALTHCHECKS_PING_URL=https://hc-ping.com/your-uuid
